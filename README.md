# odin-recipes

In this repository i will create a web page with salties and sweetest recipes, all of this are delicious. the web page will made by HTML, including text, images, and links, maybe with many pages so the project can evolve into a web page.

using git i will commit the versions on the way.